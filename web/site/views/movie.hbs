<!DOCTYPE html>
<html lang="en">
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <link rel="stylesheet" href="site.css">
        <meta charset="utf-8">
        <title>{{title}}</title>
    </head>
    <body>
        <nav class="navbar navbar-expand-sm navbar-dark bg-dark" aria-label="navbar">
            <div class="container-fluid">
                <div class="collapse navbar-collapse" >
                    <ul class="navbar-nav me-auto mb-2 mb-sm-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/popular-movies">Popular Movies</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" methods="post" href="/auth/list-groups">Groups</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/{{login}}">{{login}}</a>
                        </li>
                    </ul>
                    <form class="d-flex" role="search" action="/search-movies" method="GET">
                        <input required class="form-control me-2" type="search" name="movie-name" placeholder="Inception" aria-label="Search">
                        <button class="btn btn-outline-success" type="submit">Search</button>
                    </form>
                </div>
            </div>
        </nav>

        <p></p>
        <div class="container">
        <h2>{{title}}</h2>
        <p><img src={{image_url}} class="img-thumbnail" alt="..."></p>

        <p>Description: {{description}}</p>
        <p>Duration: {{runtimeMins}} mins</p>
        <p>Directors: {{directors}}</p>
        <p>Actors: {{actors}}</p>

        {{#if message}}
            <form action="/auth/add-movie" method="GET">
                <label>
                    <input hidden="" name="movieId" value="{{id}}">
                </label>
                <button type="submit">Add movie to group</button>
            </form>
        {{/if}}

        </div>
    </body>
</html>